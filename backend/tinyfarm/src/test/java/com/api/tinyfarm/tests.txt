- new endpoints
    - In MarketController
        @PostMapping("/buy") -> buyFromMarket in marketService
        @PostMapping("/ad")  -> publishToMarket in stockService

    - In cooperativeController
         @PostMapping("/sell") -> sellToCooperative in cooperativeService

    - In UserController
         @GetMapping("/remainingPurchases/id/{id}" -> getRemainingPurchases in UserService

- new files :
    TradeService : buy, sell, executeTrade

    DTOs : personalized object to simplify calls to the endpoints
        - CooperativeSaleRequest
        - MarketBuyRequest
        - PublishProductToTradeRequest

- added attributes :
    Cooperative : quantity
    User : remainingPurchases (reset every midnight to 12)
